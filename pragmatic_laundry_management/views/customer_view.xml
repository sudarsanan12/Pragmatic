<odoo>
    <data>
        <record id="customer_inherit" model="ir.ui.view">
            <field name="name">Customer</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='vat']" position="after">
                    <field name="is_admin" invisible="1"/>
                    <field name="is_driver" invisible="1"/>
                    <field name="is_qc" invisible="1"/>
                    <field name="is_cleaner" invisible="1"/>
                </xpath>
            </field>
        </record>

        <record id="action_customer" model="ir.actions.act_window">
            <field name="name">Customer</field>
            <field name="res_model">res.partner</field>
            <field name="view_mode">kanban,tree,form</field>
            <field name="domain">[('is_admin', '!=', True),('is_driver', '!=', True),('is_qc', '!=', True),('is_cleaner', '!=', True)]</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                Create a new Customer
                </p>
            </field>
        </record>
    </data>
</odoo>
    